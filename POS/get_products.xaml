<UserControl x:Class="POS.get_products"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"    
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS"
             mc:Ignorable="d" 
             d:DesignHeight="450"  >
    <Grid Background="White" Name="prod">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#FF1778DA" Grid.RowSpan="1">
            <Button Margin="5" Background="#FF79A6CD" HorizontalAlignment="Left"  Foreground="White"  Width="130" Click="Button_Click">
                <Grid Width="150">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="0*"/>
                        <RowDefinition Height="0*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Kind="KeyboardBackspace" VerticalAlignment="Center " RenderTransformOrigin="1.281,0.531" Margin="8,2,0,1.549"></materialDesign:PackIcon>
                    <TextBlock  FontSize="15"  Grid.ColumnSpan="2" Margin="26,0" Grid.RowSpan="2"><Run Text="Back To List">
                            <Run.Background>
                                <ImageBrush/>
                            </Run.Background>
                        </Run></TextBlock>
                </Grid>
            </Button>
            <Button Margin="5" Background="#FF79A6CD" HorizontalAlignment="Right"  Foreground="White"  Width="130" Click="Button_Click">
                <Grid Width="150">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Kind="Add" VerticalAlignment="Center " RenderTransformOrigin="1.281,0.531" Margin="8,0"></materialDesign:PackIcon>
                    <TextBlock Text="Add New"  FontSize="15" VerticalAlignment="Bottom" HorizontalAlignment="Center"  Grid.ColumnSpan="2"  Width="99"></TextBlock>
                </Grid>
            </Button>

        </Grid>
        <Grid Grid.Row="1">
            <TextBlock Text="Products" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="20" Margin="5 0 0 0"></TextBlock>
        </Grid>
        <DataGrid Grid.Row="2" x:Name="datagrid1" x:Uid="datagrid1" SelectionMode="Single"    ItemsSource="{Binding}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding id}" Header="ID" x:Name="id" IsReadOnly="True" ></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding cat_name}" Header="Cat_Name" x:Name="cat_name" ></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding pro_name}" Header="Pro_Name" x:Name="pro_name" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding quantity}" Header="Quantity" x:Name="quantity" ></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding price}" Header="Price" x:Name="price" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding date}" Header="Date" x:Name="date" ></DataGridTextColumn>
                <DataGridTemplateColumn Header="Edit">
                    <DataGridTemplateColumn.CellTemplate >
                        <DataTemplate>
                            <Button Content="Edit"  CommandParameter="{Binding id}"></Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="delete">
                    <DataGridTemplateColumn.CellTemplate >
                        <DataTemplate>
                            <Button Content="Delete" ></Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
